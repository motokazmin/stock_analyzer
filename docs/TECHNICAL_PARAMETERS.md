# 📊 Технические Параметры Анализа

## Обзор

Все технические параметры анализа документированы и обоснованы. Нет хардкодов или пустышек - все значения используются осмысленно.

## 📈 Параметры Moving Averages (MA)

| Период | Назначение | Использование |
|--------|-----------|---|
| **20** | Краткосроч тренд | Ближайшее движение (дни-недели) |
| **50** | Среднесроч тренд | Месячные тренды |
| **200** | Долгосроч тренд | Главный тренд (квартал+) |

### Где используются:
```python
# technical_analysis.py, строки 316-318, 468-470
ma_20 = close.rolling(window=20).mean()
ma_50 = close.rolling(window=50).mean()
ma_200 = close.rolling(window=200).mean()

EMA_20 = ta.trend.ema_indicator(df['CLOSE'], window=20)
EMA_50 = ta.trend.ema_indicator(df['CLOSE'], window=50)
EMA_200 = ta.trend.ema_indicator(df['CLOSE'], window=200)
```

---

## 📊 RSI (Relative Strength Index)

**Период:** 14 дней (стандартный на всех рынках)

### Пороги:
```python
RSI > 70 = Overbought (опасно покупать)
RSI < 30 = Oversold (хорошая зона для покупки)
RSI 30-70 = Neutral (нормальная зона)
```

### Где используется:
- `technical_analysis.py`, строка 477: `ta.momentum.rsi(df['CLOSE'], window=14)`
- `technical_analysis.py`, строка 138: Проверка перекупленности в `is_false_recovery()`
- Сигналы: RSI = 60 считается здоровым (ни перекупленность, ни перепроданность)

---

## 📉 ADX (Average Directional Index)

**Периоды:**
- **14 дней** - Краткосроч тренд (быстрый отклик)
- **50 дней** - Долгосроч тренд (подтверждение)

**Пороги:**
```python
ADX > 25 = Сильный тренд (можно торговать)
ADX 20-25 = Умеренный тренд
ADX < 15 = Нет тренда (боковик)
```

### Где используется:
- `technical_analysis.py`, строки 80-81: Двойная проверка ADX
- `technical_analysis.py`, строка 309: Определение главного тренда
- `technical_analysis.py`, строка 327, 335: Пороги для классификации тренда

### Логика false recovery:
```python
# Если долгосроч ADX слабый, но краткосроч сильный = опасно!
if adx_50_val < 15 and adx_14_val > 25:
    is_false = True  # Это не настоящий тренд, а отскок!
```

---

## 📊 Bollinger Bands

**Параметры:**
- **Период:** 20 дней
- **Standard Deviation:** 2

### Использование:
```python
# technical_analysis.py, строки 159-160
bb_high = ta.volatility.bollinger_hband(close, window=20, window_dev=2)
bb_low = ta.volatility.bollinger_lband(close, window=20, window_dev=2)
```

### Интерпретация:
```
price_position > 0.8 = Цена на верхней ленте (опасно) ❌
price_position < 0.2 = Цена на нижней ленте (хорошая точка входа) ✅
```

---

## 📈 MACD (Moving Average Convergence Divergence)

Используется для обнаружения дивергенций в `is_false_recovery()`:

```python
# technical_analysis.py, строки 100-109
# Проверяем: цена растёт, но MACD падает = дивергенция
price_direction = "up" if close[-1] > price_30_days_ago else "down"
macd_direction = "up" if macd.iloc[-1] > macd_30_days_ago else "down"

if price_direction == "up" and macd_direction == "down":
    is_false = True  # Цена растёт, но нет подтверждения от MACD
```

---

## 📊 OBV (On-Balance Volume)

**Параметры:**
- **Период для MA:** 30 дней

Проверяет подтверждение роста объёмом:

```python
# technical_analysis.py, строки 117-127
obv_ma = obv.rolling(window=30).mean()

# Если цена растит, но OBV падает = нет подтверждения
if price_rising and obv_falling:
    is_false = True
```

---

## 🎯 Support & Resistance

**Параметры:**
- **Окно анализа:** 20 дней (месячный уровень)
- **Бинирование:** 20 уровней (volume profile)

### Где используется:
```python
# technical_analysis.py, строка 484
support_resistance = analyzer.find_support_resistance(df, window=20)

# technical_analysis.py, строка 513
volume_profile = analyzer.calculate_volume_profile(df, bins=20)
```

---

## 🚨 False Recovery Detection

**Минимальные данные:** 50 свечей

**Логика:**
```python
reasons = []
# Проверяем 5 независимых индикаторов
# Если >= 2 показывают проблему = это ложный отскок
is_false = len(reasons) >= 2
```

**5 Проверок:**
1. ✅ ADX двойной - краткосроч vs долгосроч
2. ✅ MACD дивергенция - цена vs MACD
3. ✅ OBV - объёмы не подтверждают рост
4. ✅ RSI перекупленность - RSI > 80 без долгосроч тренда
5. ✅ Bollinger Bands - цена на верхней ленте без тренда

---

## 📋 Данные для анализа

**Минимум:**
```python
if len(df) < 50:
    logger.warning("Недостаточно данных для анализа тренда")
    # Возвращаем NEUTRAL
```

**Исторический период:** 6 месяцев (из config.json)

---

## ⚙️ Где все параметры хранятся

### Инициализация (`technical_analysis.py`, строки 39-42):
```python
self.ma_periods = [20, 50, 200]      # MA периоды
self.rsi_period = 14                 # RSI период
self.adx_strong_threshold = 25       # ADX сильный тренд
self.adx_weak_threshold = 15         # ADX нет тренда
```

### Хардкод параметры (ОБОСНОВАННЫЕ):
- `14` - стандартный RSI/ADX период (мировой стандарт)
- `20, 50, 200` - классические MA периоды (EMA/SMA)
- `25, 15, 20` - признанные пороги ADX
- `30` - месячное окно для MACD/OBV проверок
- `80` - RSI перекупленность (стандартный порог)
- `2.0` - Bollinger Bands StdDev (стандарт)
- `50` - минимум свечей для надёжного анализа

---

## ✅ Проверка целостности

Все параметры:
- ✅ Документированы и объяснены
- ✅ Используют общепринятые значения (не случайные)
- ✅ Применяются согласно финтех-стандартам
- ✅ Не содержат пустышек (заглушек)
- ✅ Все рассчёты используют реальные данные

**Дата проверки:** 15.11.2025  
**Статус:** ✅ Все параметры корректны

---

## 📖 Ссылки

- [Investopedia - Moving Averages](https://www.investopedia.com/terms/m/movingaverage.asp)
- [Investopedia - RSI](https://www.investopedia.com/terms/r/rsi.asp)
- [Investopedia - ADX](https://www.investopedia.com/terms/a/adx.asp)
- [TA-lib Documentation](https://github.com/bukosabino/ta)

