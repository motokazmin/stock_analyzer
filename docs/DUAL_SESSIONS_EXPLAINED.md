# 📊 Две записи на день - что это означает?

## 🔍 Наблюдение

Некоторые акции имеют:
- ✅ **SFIN, TATN, VKCO** - 1 запись в день (260 строк)
- ⚠️ **SOFL, SVCB, FLOT** - 2 записи в день (480-510 строк)

Пример SOFL (2025-11-10):
```
2025-11-10,85.04,85.04,78.54,79.3,26      ← ЗАПИСЬ 1 (объем 26)
2025-11-10,83.56,85.28,82.36,83.34,2150630 ← ЗАПИСЬ 2 (объем 2.1М)
```

Пример SFIN (2025-11-10):
```
2025-11-10,1279.6,1319.8,1271.0,1303.8,232914  ← ОДНА ЗАПИСЬ
```

## 📌 Причины

На Мосбирже есть **несколько торговых сессий**:

### 1️⃣ Основная сессия (T+0)
- ⏰ 10:00 - 18:45
- 📊 Стандартная торговля
- 💰 Большой объем
- 🎯 Для большинства акций

### 2️⃣ Дополнительная сессия (РПС)
- ⏰ 19:00 - 23:50
- 📊 Расширенный торговый час
- 💰 Маленький объем
- 🎯 Для избранных акций (SOFL, SVCB, FLOT)

## 🎯 Что это означает

| Акция | Сессии | Что значит |
|-------|--------|-----------|
| **SFIN** | Только основная | Стандартная |
| **SOFL** | Основная + РПС | Повышенная ликвидность |
| **SVCB** | Основная + РПС | Повышенная ликвидность |
| **FLOT** | Основная + РПС | Повышенная ликвидность |

## 🔴 ПРОБЛЕМА

В CSV есть:
```
2025-11-10, CLOSE=79.3, V=26        ← РПС (дополнительная)
2025-11-10, CLOSE=83.34, V=2150630  ← T+0 (основная)
```

**Две разные цены закрытия в один день!**

Какую использовать для анализа?
- ❓ Закрытие РПС (79.3)?
- ❓ Закрытие T+0 (83.34)?
- ❓ Или объединить как-то?

## ✅ РЕШЕНИЕ

### Вариант 1: Использовать ОСНОВНУЮ сессию (T+0)
```python
# Когда есть две записи за день, берем вторую (основную)
df = df.sort_values(['DATE', 'VOLUME'], ascending=[True, False])
df = df.drop_duplicates(subset=['DATE'], keep='first')  # Берем с большим объемом
```

**Результат:** CLOSE=83.34 (основная сессия)

**Плюсы:**
- ✅ Стандартный закрывающий курс
- ✅ Большой объем = ликвиднее
- ✅ Согласуется с T+0 котировками

### Вариант 2: Использовать ДОПОЛНИТЕЛЬНУЮ сессию (РПС)
```python
# Берем первую запись (дополнительная)
df = df.drop_duplicates(subset=['DATE'], keep='first')  # РПС
```

**Результат:** CLOSE=79.3 (дополнительная сессия)

**Минусы:**
- ❌ Нетипичный курс
- ❌ Маленький объем
- ❌ Не совпадает с котировками T+0

### Вариант 3: Объединить обе сессии
```python
# Группируем по дате, суммируем объемы, берем взвешенное среднее
df_grouped = df.groupby('DATE').agg({
    'OPEN': 'first',      # Открытие первой сессии
    'HIGH': 'max',        # Максимум за обе сессии
    'LOW': 'min',         # Минимум за обе сессии
    'CLOSE': lambda x: (x * volumes).sum() / volumes.sum(),  # Взвешенное закрытие
    'VOLUME': 'sum'       # Суммарный объем
})
```

**Результат:** Объединенные свечи

**Минусы:**
- ❌ Искусственные данные
- ❌ Не отражают реальность

## 🎯 РЕКОМЕНДАЦИЯ

**Использовать Вариант 1: Основную сессию (T+0)**

Причины:
- ✅ T+0 - стандарт на Мосбирже
- ✅ Совпадает с обычными котировками
- ✅ Большой объем = точнее
- ✅ Просто (одна запись в день)

---

## 🔧 Код исправления

```python
# fix_csv_issues.py (обновленная версия)

def fix_csv_file(csv_path):
    df = pd.read_csv(csv_path, parse_dates=['DATE'])
    
    # Если есть дублирующиеся даты - берем сессию с большим объемом (T+0)
    if df.duplicated(subset=['DATE']).any():
        df = df.sort_values(['DATE', 'VOLUME'], ascending=[True, False])
        df = df.drop_duplicates(subset=['DATE'], keep='first')
        logger.info(f"Объединены двойные сессии: осталась основная (T+0)")
    
    df.to_csv(csv_path, index=False)
```

---

## 📊 После исправления

**SOFL было:**
```
2025-11-10: две записи
2025-11-11: две записи
2025-11-12: две записи
...
Всего: 512 строк
```

**SOFL станет:**
```
2025-11-10: одна запись (T+0, CLOSE=83.34, V=2150630)
2025-11-11: одна запись (T+0)
2025-11-12: одна запись (T+0)
...
Всего: 256 строк
```

---

## ✨ Итог

**Что это означает:** На Мосбирже некоторые акции (SOFL, SVCB, FLOT) торгуются в двух сессиях - основной и дополнительной.

**Что делать:**
1. Оставить основную сессию (T+0)
2. Удалить дополнительную (РПС)
3. Получить чистые данные - одна запись в день

**Результат:** Все 22 акции будут одинаково обработаны ✅

