# Установка и Настройка

## Требования системы

- **Python**: 3.7+
- **ОС**: Linux, Mac, Windows
- **Интернет**: Подключение для доступа к API Мосбиржи

## Пошаговая установка

### Шаг 1: Клонирование/Подготовка

```bash
cd /home/roman/projects/ai/trading
```

### Шаг 2: Создание виртуального окружения (опционально, но рекомендуется)

```bash
# Linux/Mac
python3 -m venv venv
source venv/bin/activate

# Windows
python -m venv venv
venv\Scripts\activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

Зависимости:
- **requests** (2.31.0) - HTTP клиент для API
- **pandas** (2.0.3) - Работа с данными
- **urllib3** (2.0.4) - HTTP утилиты

### Шаг 4: Проверка установки

```bash
python test_manager.py
```

Если все тесты пройдены ✓, установка успешна!

## Структура проекта

```
trading/
├── stock_data_manager.py      # Основной класс (395 строк)
├── data_analyzer.py           # Анализатор данных (280 строк)
├── config.py                  # Конфигурация
├── example_usage.py           # 5 примеров использования
├── test_manager.py            # Тесты (5 тестов)
├── daily_update.py            # Ежедневное обновление
├── requirements.txt           # Зависимости Python
├── README.md                  # Полная документация
├── QUICKSTART.md              # Быстрый старт
├── INSTALL.md                 # Этот файл
├── stock_data_manager.log     # Логи (создается автоматически)
└── stock_data/                # Данные (создается автоматически)
    ├── SBER_full.csv
    ├── GAZP_full.csv
    └── ...
```

## Используемые библиотеки

### requests
- Отправка HTTP запросов к API Мосбиржи
- Автоматические повторные попытки при сбое
- Кэширование соединений

### pandas
- Обработка данных в табличном формате
- Операции с датами и временем
- Экспорт в CSV

### urllib3
- Низкоуровневые HTTP утилиты
- Пулинг соединений
- Поддержка SSL

## Конфигурация

Основные параметры в `config.py`:

```python
# API
MOEX_API_BASE = "https://iss.moex.com/iss/history/engines/stock/markets/shares/securities"
BATCH_SIZE = 100                    # Записей за запрос
REQUEST_TIMEOUT = 10                # Таймаут (сек)
MAX_RETRIES = 3                     # Повторы при ошибке

# Данные
DATA_DIR = Path("stock_data")       # Папка для CSV
LOG_FILE = "stock_data_manager.log" # Файл логов

# Избранные акции
DEFAULT_WATCHLIST = ['SBER', 'GAZP', 'LKOH', 'NVTK', 'TATN']

# Период
INITIAL_PERIOD_DAYS = 365           # Дни при инициальной загрузке
```

## Первый запуск

### Быстрый тест (1 минута)

```bash
python test_manager.py
```

### Загрузка данных (5-10 минут)

```bash
python example_usage.py
```

Отредактируйте файл и раскомментируйте нужный пример:

```python
if __name__ == "__main__":
    example_2_update_watchlist()  # ← Раскомментируйте
```

### Использование в коде

```python
from stock_data_manager import StockDataManager

manager = StockDataManager()

# Обновить 5 популярных акций
manager.update_watchlist(['SBER', 'GAZP', 'LKOH', 'NVTK', 'TATN'])

# Получить данные
data = manager.get_data('SBER')
print(data.head())

# Статистика
stats = manager.get_statistics('SBER')
print(f"Ср. цена: {stats['avg_price']:.2f}")
```

## Автоматизация

### Linux/Mac: cron

1. Откройте crontab:
```bash
crontab -e
```

2. Добавьте строку (обновление в 11:00 каждый день):
```bash
0 11 * * * cd /home/roman/projects/ai/trading && python daily_update.py
```

3. Проверьте логи:
```bash
tail stock_data_manager.log
```

### Windows: Task Scheduler

1. Создайте задачу в Task Scheduler
2. Укажите программу: `python.exe`
3. Аргументы: `daily_update.py`
4. Рабочая папка: `/home/roman/projects/ai/trading`
5. Расписание: каждый день в 11:00

## Логирование

### Просмотр логов

```bash
# Последние 20 строк
tail -n 20 stock_data_manager.log

# Все ошибки
grep ERROR stock_data_manager.log

# Все операции за последний час
grep "$(date -d '1 hour ago' '+%H:%M')" stock_data_manager.log

# В реальном времени
tail -f stock_data_manager.log
```

### Уровни логирования

- **INFO** - нормальные операции
- **WARNING** - предупреждения (например, файл пуст)
- **ERROR** - ошибки при загрузке
- **DEBUG** - подробная отладочная информация

## Решение проблем

### Ошибка: "No module named 'pandas'"

```bash
pip install -r requirements.txt
```

### Ошибка: "ConnectionError: Max retries exceeded"

1. Проверьте интернет-соединение
2. Проверьте, не забанена ли ваша IP
3. Подождите несколько минут и повторите

### Ошибка: "KeyError: 'history'"

API вернул неожиданный ответ. Проверьте:
1. Правильность тикера (например, 'SBER', 'GAZP')
2. Доступность сервиса https://iss.moex.com/
3. Логи в `stock_data_manager.log`

### API возвращает пустой ответ

- Возможно, тикер неправильный
- Возможно, акция была удалена из листинга
- Проверьте на сайте https://www.moex.com/

### Очень медленная загрузка

- Это нормально для первой загрузки (много данных)
- Используйте меньший период в `INITIAL_PERIOD_DAYS`
- Добавьте интервалы между запросами

## Производительность

### Типичные времена операций

- **Первая загрузка одной акции**: 10-30 сек
- **Обновление 5 акций**: 30-60 сек
- **Загрузка из CSV**: <1 сек
- **Статистика**: <1 сек

### Оптимизация

1. **Кэширование**: Используйте сохраненные CSV вместо повторных загрузок
2. **Батчинг**: Обновляйте несколько акций одновременно
3. **Расписание**: Запускайте обновления в нерабочие часы

## Безопасность

- Скрипт **не требует** ключей или аутентификации
- API Мосбиржи публичный и бесплатный
- Данные сохраняются **локально** в CSV
- Все операции логируются

## Обновление

### Обновление зависимостей

```bash
pip install --upgrade -r requirements.txt
```

### Обновление кода

Загрузите новую версию с репозитория.

## Деинсталляция

```bash
# Удалить виртуальное окружение
rm -rf venv

# Удалить загруженные данные
rm -rf stock_data/

# Удалить логи
rm stock_data_manager.log
```

## Получение помощи

1. Прочитайте `README.md` для полной документации
2. Смотрите примеры в `example_usage.py`
3. Запустите тесты: `python test_manager.py`
4. Проверьте логи: `tail stock_data_manager.log`

## Что дальше?

✓ Установка завершена  
✓ Протестировано  
✓ Готово к использованию  

**Начните:**
```bash
python example_usage.py
```

Или используйте в своем коде - смотрите `README.md`!

---

**Документация:**
- 📖 **README.md** - полная документация API
- 🚀 **QUICKSTART.md** - быстрый старт
- 💻 **example_usage.py** - примеры кода
- ✅ **test_manager.py** - тесты

