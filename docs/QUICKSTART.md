# Быстрый Старт

## Установка (2 минуты)

```bash
# 1. Перейдите в директорию проекта
cd /home/roman/projects/ai/trading

# 2. Установите зависимости
pip install -r requirements.txt
```

## Первый запуск

### Вариант 1: Обновить популярные акции

```bash
python example_usage.py
```

Отредактируйте файл `example_usage.py` и раскомментируйте нужный пример в функции `main()`.

### Вариант 2: Запустить тесты

```bash
python test_manager.py
```

Это протестирует все компоненты системы.

### Вариант 3: Использовать в своем коде

```python
from stock_data_manager import StockDataManager

manager = StockDataManager()

# Обновить список акций
manager.update_watchlist(['SBER', 'GAZP', 'LKOH'])

# Получить данные
df = manager.get_data('SBER')
print(df.tail())

# Получить статистику
stats = manager.get_statistics('SBER')
print(f"Средняя цена: {stats['avg_price']:.2f} ₽")
```

## Основные команды

### Загрузить данные одной акции

```python
manager = StockDataManager()
data = manager.download_stock_data('SBER', '2024-01-01', '2024-12-31')
manager.save_to_csv('SBER', data)
```

### Обновить список акций (инкрементально)

```python
manager = StockDataManager()
manager.update_watchlist(['SBER', 'GAZP', 'LKOH'])
```

### Получить последние данные

```python
df = manager.get_data('SBER')
print(df.tail(10))  # Последние 10 дней
```

### Получить статистику

```python
stats = manager.get_statistics('SBER')
for key, value in stats.items():
    print(f"{key}: {value}")
```

## Файловая структура

После первого запуска будет создана структура:

```
trading/
├── stock_data/              # Папка с данными
│   ├── SBER_full.csv
│   ├── GAZP_full.csv
│   └── ...
├── stock_data_manager.log   # Логи
├── stock_data_manager.py    # Основной файл
├── example_usage.py         # Примеры
├── test_manager.py          # Тесты
└── requirements.txt         # Зависимости
```

## Тикеры популярных акций

| Компания | Тикер | Вид |
|----------|-------|-----|
| Сбербанк | SBER | Финансовый |
| Газпром | GAZP | Энергетика |
| Лукойл | LKOH | Нефть и газ |
| Новатэк | NVTK | Нефть и газ |
| Татнефть | TATN | Нефть и газ |
| Полюс Золото | PLZL | Драгметаллы |
| Фармакор | PHOR | Фармацевтика |

## Логирование

Все логи сохраняются в файл `stock_data_manager.log`:

```bash
# Просмотреть последние логи
tail -f stock_data_manager.log

# Просмотреть только ошибки
grep ERROR stock_data_manager.log
```

## Решение проблем

### Ошибка: "ConnectionError"
Проверьте интернет-соединение. Менеджер автоматически повторит попытку.

### Ошибка: "No module named 'requests'"
```bash
pip install -r requirements.txt
```

### API возвращает пустой ответ
Возможно, тикер неправильный или акция неактивна. Проверьте список активных тикеров на сайте Мосбиржи.

## Автоматизация

### Linux/Mac: cron

Добавьте в crontab:

```bash
crontab -e

# Добавьте строку (обновление каждый день в 11:00)
0 11 * * * cd /home/roman/projects/ai/trading && python -c "from stock_data_manager import StockDataManager; m = StockDataManager(); m.update_watchlist(['SBER', 'GAZP', 'LKOH'])"
```

### Windows: Task Scheduler

Создайте файл `daily_update.py`:

```python
from stock_data_manager import StockDataManager

manager = StockDataManager()
manager.update_watchlist(['SBER', 'GAZP', 'LKOH', 'NVTK', 'TATN'])
```

И запланируйте его через Task Scheduler.

## Дополнительно

📖 **Полная документация**: смотрите `README.md`  
💻 **Примеры кода**: смотрите `example_usage.py`  
✅ **Тесты**: запустите `python test_manager.py`

## Поддержка

Если возникли проблемы:
1. Проверьте логи в `stock_data_manager.log`
2. Убедитесь, что установлены все зависимости
3. Проверьте интернет-соединение
4. Используйте `test_manager.py` для диагностики

Готово! 🚀 Вы готовы к работе с данными Мосбиржи!

