# 📊 Report Generator - Генератор отчётов

Модуль для автоматической генерации красивых markdown-отчётов с анализом акций, сигналами и рекомендациями.

## 🚀 Быстрый старт

```python
from report_generator import ReportGenerator

generator = ReportGenerator()

# Генерируем отчёт
tickers = ['SBER', 'GAZP', 'LKOH']
filepath = generator.generate_and_save(tickers)

print(f"Отчёт сохранён: {filepath}")
```

## 📚 Основные функции

### 1. `generate_weekly_report(tickers)`

Генерирует полный еженедельный отчёт.

**Параметры:**
- `tickers` - список тикеров (например, ['SBER', 'GAZP', 'LKOH'])

**Возвращает:** Строка с markdown-отчётом

**Содержание отчёта:**
- Заголовок с датой
- Таблица рейтинга всех акций
- Главные сигналы (BUY/SELL/HOLD)
- Детальный анализ каждой акции

**Пример:**
```python
report = generator.generate_weekly_report(['SBER', 'GAZP'])
print(report)
```

---

### 2. `rank_stocks(analysis_results)`

Ранжирует акции по качеству сигнала.

**Параметры:**
- `analysis_results` - список результатов от `analyze_stock()`

**Возвращает:** Отсортированный список с рейтингом и скором

**Система скоринга:**
- **+40 баллов** - сильный восходящий тренд
- **+30 баллов** - RSI < 30 (перепродано)
- **+20 баллов** - цена выше MA20 и MA50
- **+10 баллов** - растущие объёмы
- **-20 баллов** - сильный нисходящий тренд

**Структура результата:**
```python
[
    {
        'rank': 1,              # Место в рейтинге
        'ticker': 'SBER',
        'score': 85,            # Общий скор
        'price': 105.50,
        'price_change': 2.5,
        'rsi': 45.5,
        'trend': 'up',
        'factors': [            # Факторы, повлиявшие на скор
            'Сильный восход. тренд (+40)',
            'RSI нейтральный (+20)',
            ...
        ],
        'full_result': {...}    # Полный результат анализа
    },
    ...
]
```

**Пример:**
```python
from technical_analysis import TechnicalAnalyzer

analyzer = TechnicalAnalyzer()
results = [analyzer.analyze_stock(ticker) for ticker in tickers]
ranked = generator.rank_stocks(results)

# Топ 3 акции
for item in ranked[:3]:
    print(f"{item['rank']}. {item['ticker']} - скор {item['score']}")
```

---

### 3. `find_signals(analysis_result)`

Находит торговые сигналы на основе анализа.

**Параметры:**
- `analysis_result` - результат от `analyze_stock()`

**Возвращает:** Dict с сигналами

**Структура результата:**
```python
{
    'primary': '🟢 ПОКУПКА',              # Основной сигнал
    'strength': 'strong',                  # Сила сигнала
    'indicators': [                        # Список индикаторов
        '🟢 RSI < 30 (перепродано)',
        'Цена выше обоих МА'
    ],
    'conditions': [...]                    # Выполненные условия
}
```

**Сигналы:**
- **🟢 ПОКУПКА** (strong) - RSI < 30 И восходящий тренд
- **🔴 ПРОДАЖА** (strong) - RSI > 70 И нисходящий тренд
- **🟡 ОСТОРОЖНОСТЬ** (moderate) - противоречивые сигналы
- **⚪ НЕЙТРАЛЬНО** (weak) - неопределённое состояние

**Пример:**
```python
signals = generator.find_signals(result)

if signals['primary'] == '🟢 ПОКУПКА':
    print("Рекомендуется покупка")
```

---

### 4. `save_report(report_text, filename=None)`

Сохраняет отчёт в markdown-файл.

**Параметры:**
- `report_text` - текст отчёта
- `filename` - имя файла (если None, использует дату)

**Возвращает:** Path к файлу

**Примеры имён файлов:**
- `report_20240101_101530.md` (автоматическое)
- `report_portfolio.md` (кастомное)
- `report_top5.md` (кастомное)

**Пример:**
```python
report = generator.generate_weekly_report(tickers)
filepath = generator.save_report(report, 'report_monday.md')
print(f"Сохранён: {filepath}")
```

---

### 5. `generate_and_save(tickers, filename=None)`

**ГЛАВНЫЙ МЕТОД** - генерирует и сохраняет отчёт в одном вызове.

**Параметры:**
- `tickers` - список тикеров
- `filename` - имя файла (опционально)

**Возвращает:** Path к файлу

**Пример:**
```python
filepath = generator.generate_and_save(['SBER', 'GAZP', 'LKOH'])
# Отчёт автоматически сохранён в reports/report_20240101_101530.md
```

---

## 📖 Примеры использования

### Пример 1: Простой отчёт

```python
from report_generator import ReportGenerator

generator = ReportGenerator()

# Генерируем отчёт для топ акций
tickers = ['SBER', 'GAZP', 'LKOH', 'NVTK', 'TATN']
filepath = generator.generate_and_save(tickers)

print(f"✅ Отчёт: {filepath}")
```

### Пример 2: Кастомное имя

```python
from datetime import datetime

generator = ReportGenerator()

now = datetime.now()
name = f"report_daily_{now.strftime('%Y%m%d')}.md"

filepath = generator.generate_and_save(
    ['SBER', 'GAZP'],
    filename=name
)
```

### Пример 3: Ранжирование и фильтр

```python
from technical_analysis import TechnicalAnalyzer

analyzer = TechnicalAnalyzer()
generator = ReportGenerator()

# Анализируем
tickers = ['SBER', 'GAZP', 'LKOH', 'NVTK', 'TATN', 'PLZL']
results = [analyzer.analyze_stock(t) for t in tickers]

# Ранжируем
ranked = generator.rank_stocks(results)

# Топ 3 акции
top3 = ranked[:3]
print("\n🏆 ТОП 3 АКЦИИ:")
for item in top3:
    print(f"{item['rank']}. {item['ticker']} (скор: {item['score']})")

# Только они в отчёт
top3_tickers = [item['ticker'] for item in top3]
filepath = generator.generate_and_save(top3_tickers, 'report_top3.md')
```

### Пример 4: Сигналы на покупку

```python
from technical_analysis import TechnicalAnalyzer

analyzer = TechnicalAnalyzer()
generator = ReportGenerator()

tickers = ['SBER', 'GAZP', 'LKOH', 'NVTK', 'TATN']

print("\n🟢 СИГНАЛЫ НА ПОКУПКУ:")
print("─" * 60)

for ticker in tickers:
    result = analyzer.analyze_stock(ticker)
    signals = generator.find_signals(result)
    
    if signals['primary'] == '🟢 ПОКУПКА':
        print(f"\n{ticker}")
        print(f"  Сила: {signals['strength']}")
        for ind in signals['indicators']:
            print(f"  • {ind}")
```

### Пример 5: Точки входа/выхода

```python
generator = ReportGenerator()
result = analyzer.analyze_stock('SBER')

# Детальный анализ
detailed = generator.generate_detailed_analysis(result)
print(detailed)

# Содержит:
# - Точки входа (по поддержке)
# - Цели прибыли (по сопротивлению)
# - Стоп-лоссы
```

---

## 📊 Формат отчёта

### Структура

```markdown
# Еженедельный анализ акций

**Дата:** 13.11.2024  
**Неделя:** 11.11.2024 - 13.11.2024  
**Проанализировано акций:** 5

## 🏆 Рейтинг акций

| # | Тикер | Цена | Изм% | RSI | Тренд | Сигнал | Скор | Комментарий |
|---|-------|------|------|-----|-------|--------|------|-------------|
| 1 | SBER | 105.50 | +2.5% | 45 | UP | 🟢 BUY | 85 | Сильный восход. тренд |
| ... |

## 📊 Главные сигналы

### 🟢 Сигналы на ПОКУПКУ
- **SBER** (скор: 85) - Сильный восходящий тренд...
- ...

### 🔴 Сигналы на ПРОДАЖУ
- ...

### 🟡 HOLD
- Остальные акции

## 📈 Детальный анализ

### SBER - Детальный анализ

#### Базовая информация
- **Текущая цена:** 105.50 ₽
- ...

#### Сигнал
**🟢 ПОКУПКА** (strong)
- 🟢 RSI < 30
- Цена выше обоих МА

#### Технические индикаторы
- **EMA 20:** 104.30
- **RSI:** 45.0 (neutral)
- ...

#### Точки входа
- На откате к поддержке: 100.50 ₽

#### Цели прибыли
- Первая цель (Сопротивление): 110.50 ₽ (+4.8%)

#### Стоп-лосс
- На уровне поддержки: 100.50 ₽ (-4.8%)

#### Вывод
✅ Рекомендация: Подходит для долгосрочного входа.
```

---

## 🗂️ Директория отчётов

Все отчёты сохраняются в `reports/` директории:

```
stock_analyzer/
├── reports/
│   ├── report_20240113_101530.md
│   ├── report_top3.md
│   ├── report_portfolio.md
│   └── ...
```

---

## 🎯 Интеграция с остальными модулями

### С stock_data_manager

```python
from stock_data_manager import StockDataManager
from report_generator import ReportGenerator

# Обновляем данные
manager = StockDataManager()
manager.update_watchlist(['SBER', 'GAZP', 'LKOH'])

# Генерируем отчёт
generator = ReportGenerator()
filepath = generator.generate_and_save(['SBER', 'GAZP', 'LKOH'])
```

### С technical_analysis

```python
from technical_analysis import TechnicalAnalyzer
from report_generator import ReportGenerator

analyzer = TechnicalAnalyzer()
generator = ReportGenerator()

# Полный цикл
result = analyzer.analyze_stock('SBER')
signals = generator.find_signals(result)
print(signals)
```

---

## ⚙️ Запуск примеров

```bash
python report_example.py
```

Там 7 полных примеров:
1. Простой отчёт
2. Сохранение отчёта
3. Кастомное имя
4. Ранжирование
5. Анализ сигналов
6. Сравнение отчётов
7. Точки входа/выхода

---

## 📈 Интерпретация скора

| Скор | Статус | Рекомендация |
|------|--------|--------------|
| **> 70** | 🟢 ОЧЕНЬ ХОРОШО | Сильный сигнал на покупку |
| **50-70** | 🟢 ХОРОШО | Рекомендуется покупка |
| **30-50** | 🟡 НЕЙТРАЛЬНО | Подождать лучшего входа |
| **10-30** | 🟡 СЛАБО | Избегать или ждать |
| **< 10** | 🔴 ПЛОХО | Не рекомендуется |

---

## 🔧 Рекомендации

1. **Генерируйте отчёты** каждую неделю или день
2. **Сравнивайте** отчёты за разные периоды
3. **Используйте рейтинг** для выбора акций
4. **Проверяйте сигналы** перед торговлей
5. **Комбинируйте** с собственным анализом

---

## 📝 Примечания

- Отчёты сохраняются в UTF-8 кодировке
- Используются emoji для наглядности
- Все цены в рублях (₽)
- Скоринг основан на техническом анализе

---

**Версия:** 1.0.0  
**Дата:** 13 Ноября 2024  
**Статус:** ✅ Production Ready

