# 💻 CLI Guide - Командный интерфейс Stock Analyzer

Полный интерфейс командной строки для управления и анализа акций.

## 🚀 Быстрый старт

```bash
# 1. Показать текущий watchlist
python main.py list

# 2. Обновить данные
python main.py update

# 3. Создать анализ
python main.py analyze

# 4. Смотреть результаты в reports/report_*.md
```

## 📋 Все команды

### 1. `python main.py update`

**Обновляет данные всех акций из watchlist.**

```bash
$ python main.py update

============================================================
📥 ОБНОВЛЕНИЕ ДАННЫХ АКЦИЙ
============================================================

📊 Обновляем 5 акций: SBER, GAZP, LKOH, NVTK, TATN

✅ Успешно: 5
❌ Ошибок: 0

✅ Все данные обновлены!
```

**Что происходит:**
1. Загружаются последние данные с API Мосбиржи
2. Обновляются CSV файлы в `stock_data/`
3. Записывается время последнего обновления в `config.json`

**CSV файлы:**
```
stock_data/
├── SBER_full.csv
├── GAZP_full.csv
├── LKOH_full.csv
├── NVTK_full.csv
└── TATN_full.csv
```

---

### 2. `python main.py analyze`

**Проводит полный технический анализ и создаёт отчёт.**

```bash
$ python main.py analyze

============================================================
📊 АНАЛИЗ И СОЗДАНИЕ ОТЧЁТА
============================================================

🔍 Анализируем 5 акций...

✅ Отчёт создан: reports/report_20240113_101530.md

📄 Первая часть отчёта:
────────────────────────────────────────────────────────────
# Еженедельный анализ акций

**Дата:** 13.11.2024
...
────────────────────────────────────────────────────────────
```

**Что происходит:**
1. Анализирует каждую акцию (EMA, RSI, тренд, объёмы)
2. Ранжирует акции по скору
3. Создаёт подробный markdown-отчёт
4. Сохраняет в `reports/report_TIMESTAMP.md`

**Содержание отчёта:**
- Рейтинг всех акций в таблице
- Сигналы на покупку/продажу
- Детальный анализ каждой акции
- Точки входа, стоп-лоссы, цели прибыли

---

### 3. `python main.py add <TICKER>`

**Добавляет акцию в watchlist.**

```bash
$ python main.py add PLZL

➕ Добавляем PLZL...

🔍 Проверяем PLZL...
✅ PLZL найден!

✅ PLZL добавлен в watchlist
```

**Что происходит:**
1. Проверяет, существует ли такая акция
2. Проверяет наличие данных
3. Добавляет в `config.json`
4. Обновляет список

**Примеры:**
```bash
python main.py add SBER      # Добавить Сбербанк
python main.py add GAZP      # Добавить Газпром
python main.py add LKOH      # Добавить Лукойл
python main.py add plzl      # Работает в любом регистре
```

---

### 4. `python main.py remove <TICKER>`

**Удаляет акцию из watchlist.**

```bash
$ python main.py remove TATN

➖ Удаляем TATN...

✅ TATN удален из watchlist
```

**Примеры:**
```bash
python main.py remove NVTK
python main.py remove GAZP
```

---

### 5. `python main.py list`

**Показывает текущий watchlist.**

```bash
$ python main.py list

============================================================
📋 ТЕКУЩИЙ WATCHLIST
============================================================

Отслеживаем 5 акций:

  1. SBER
  2. GAZP
  3. LKOH
  4. NVTK
  5. TATN
```

---

### 6. `python main.py info <TICKER>`

**Показывает детальную информацию по акции.**

```bash
$ python main.py info SBER

============================================================
📊 ИНФОРМАЦИЯ ПО SBER
============================================================

Цена: 105.50 ₽
Изменение: +5.50 (+5.50%)
Период: 2023-11-13 - 2024-11-13
Данных: 252 дней

Технические индикаторы:
  EMA 20: 104.30
  EMA 50: 103.20
  RSI: 45.00 (neutral)

Тренд:
  Направление: UP
  Сила: STRONG
  Выше MA20: ✅ Да
  Выше MA50: ✅ Да

Сигнал: 🟢 ПОКУПКА (strong)
```

---

### 7. `python main.py status`

**Показывает статус приложения.**

```bash
$ python main.py status

============================================================
📈 СТАТУС STOCK ANALYZER
============================================================

Информация о приложении:
  Версия: 1.0.0
  Статус: ✅ Активно

Конфигурация:
  Акций в watchlist: 5
  Список: SBER, GAZP, LKOH, NVTK, TATN
  Последнее обновление: 2024-11-13T10:15:30
  Последний отчёт: 2024-11-13T10:16:45

Доступные данные:
  CSV файлов: 5
    • SBER_full.csv (245.3 KB)
    • GAZP_full.csv (198.5 KB)
    • LKOH_full.csv (187.2 KB)
    • NVTK_full.csv (156.8 KB)
    • TATN_full.csv (142.1 KB)

Отчёты:
  Markdown отчётов: 8
    Последний: report_20240113_101530.md
```

---

## 🔧 Файл конфигурации (config.json)

### Структура

```json
{
  "watchlist": [
    "SBER",
    "GAZP",
    "LKOH",
    "NVTK",
    "TATN"
  ],
  "last_updated": "2024-11-13T10:15:30",
  "last_report": "2024-11-13T10:16:45",
  "settings": {
    "auto_update": false,
    "report_format": "markdown",
    "theme": "default"
  }
}
```

### Полей конфигурации

| Поле | Тип | Описание |
|------|-----|---------|
| **watchlist** | List[str] | Список отслеживаемых акций |
| **last_updated** | str \| null | ISO дата последнего обновления |
| **last_report** | str \| null | ISO дата последнего отчёта |
| **settings.auto_update** | bool | Автоматическое обновление (зарезервировано) |
| **settings.report_format** | str | Формат отчёта (markdown/json/csv) |
| **settings.theme** | str | Тема оформления |

### Редактирование вручную

Вы можете отредактировать `config.json` прямо:

```json
{
  "watchlist": [
    "SBER",
    "GAZP",
    "PLZL"
  ]
}
```

---

## 📖 Примеры использования

### Пример 1: Полный рабочий цикл

```bash
# Проверяем текущий список
python main.py list

# Обновляем данные
python main.py update

# Анализируем и создаём отчёт
python main.py analyze

# Проверяем статус
python main.py status
```

### Пример 2: Изменение watchlist

```bash
# Добавляем новую акцию
python main.py add PLZL

# Показываем новый список
python main.py list

# Удаляем акцию
python main.py remove TATN

# Подтверждаем изменения
python main.py list
```

### Пример 3: Анализ конкретной акции

```bash
# Смотрим информацию
python main.py info GAZP

# Обновляем её данные
python main.py update

# Анализируем
python main.py analyze

# Ищем в отчёте анализ GAZP
```

### Пример 4: Регулярное использование

```bash
# Ежедневно (например, в cron):
python main.py update && python main.py analyze

# Результат будет в reports/
```

---

## 🎯 Типичные сценарии

### Сценарий 1: Настройка на неделю

```bash
# Пн: Устанавливаем portfolio
python main.py add SBER
python main.py add GAZP
python main.py add LKOH

# Вт-Пт: Ежедневное обновление
python main.py update && python main.py analyze

# Пт: Окончательный отчёт
python main.py status
```

### Сценарий 2: Мониторинг конкретных акций

```bash
# Добавляем интересующие
python main.py add PLZL
python main.py add PHOR

# Смотрим информацию
python main.py info PLZL
python main.py info PHOR

# Обновляем когда нужно
python main.py update
```

### Сценарий 3: Автоматизация

```bash
# Linux/Mac: добавить в crontab
# Ежедневно в 11:00
0 11 * * * cd /path/to/stock_analyzer && python main.py update && python main.py analyze
```

---

## 🐛 Решение проблем

### Ошибка: "config.json не найден"

**Решение:** Автоматически создастся при первом запуске. Если нет:

```bash
python main.py list  # Создаст config.json
```

### Ошибка: "Акция не найдена"

**Решение:** Проверьте правильность тикера:

```bash
# Неправильно
python main.py add XYZ123

# Правильно
python main.py add GAZP
```

### Ошибка: "Нет данных"

**Решение:** Сначала обновите данные:

```bash
python main.py update
python main.py analyze
```

### Отчёт не создаётся

**Решение:** Проверьте статус:

```bash
python main.py status
python main.py list
```

---

## 📊 Выходные файлы

### Структура директорий

```
stock_analyzer/
├── config.json           # Конфигурация
├── main.py              # CLI интерфейс
├── stock_data/          # CSV данные
│   ├── SBER_full.csv
│   └── ...
└── reports/             # Markdown отчёты
    ├── report_20240113_101530.md
    └── ...
```

### Типы отчётов

- **report_TIMESTAMP.md** - полный еженедельный анализ
- **report_daily_DATE.md** - ежедневный отчёт
- **report_top3.md** - кастомный отчёт

---

## 🔌 Интеграция

### С другими скриптами

```python
from main import ConfigManager, StockAnalyzerCLI

# Получить watchlist
tickers = ConfigManager.get_watchlist()

# Использовать CLI методы
cli = StockAnalyzerCLI()
cli.update_data(None)
```

---

## 📞 Справка

```bash
python main.py -h
python main.py update -h
python main.py add -h
```

---

## ✨ Особенности

✅ Простой интерфейс командной строки  
✅ Автоматическое управление конфигурацией  
✅ JSON хранилище параметров  
✅ Подробные сообщения об ошибках  
✅ Полная интеграция с анализом  
✅ Поддержка любого количества акций  

---

**Версия:** 1.0.0  
**Дата:** 13 Ноября 2024  
**Статус:** ✅ Production Ready

