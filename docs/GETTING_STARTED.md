# 🚀 Getting Started - Начало работы

Полное руководство по запуску Stock Analyzer.

## ⚡ За 5 минут

### Шаг 1: Установка

```bash
pip install -r requirements.txt
```

### Шаг 2: Первый запуск

```bash
# Проверить конфигурацию
python main.py status

# Обновить данные
python main.py update

# Создать отчёт
python main.py analyze
```

### Шаг 3: Результаты

Откройте отчёт в `reports/report_*.md` в любом текстовом редакторе или браузере.

---

## 📖 Подробно

### Установка (5 минут)

1. **Убедитесь, что установлены:**
   - Python 3.7+
   - pip

2. **Установите зависимости:**
   ```bash
   cd /home/roman/projects/ai/stock_analyzer
   pip install -r requirements.txt
   ```

3. **Проверьте установку:**
   ```bash
   python -c "import pandas, numpy, requests; print('✅ OK')"
   ```

### Первый запуск

```bash
# 1. Показать справку
python main.py -h

# 2. Показать текущий список акций
python main.py list

# 3. Обновить данные (займёт несколько минут)
python main.py update

# 4. Создать анализ (займёт несколько минут)
python main.py analyze

# 5. Проверить результаты
python main.py status
```

### Управление списком акций

```bash
# Показать текущий список
python main.py list

# Добавить акцию
python main.py add PLZL

# Удалить акцию
python main.py remove TATN

# Показать информацию по акции
python main.py info GAZP
```

---

## 🎯 Основные команды

| Команда | Описание | Примеры |
|---------|---------|---------|
| `update` | Обновить данные | `python main.py update` |
| `analyze` | Создать анализ | `python main.py analyze` |
| `add` | Добавить акцию | `python main.py add SBER` |
| `remove` | Удалить акцию | `python main.py remove GAZP` |
| `list` | Показать список | `python main.py list` |
| `info` | Информация по акции | `python main.py info LKOH` |
| `status` | Статус приложения | `python main.py status` |

---

## 📁 Структура файлов

```
stock_analyzer/
├── main.py                          # CLI интерфейс (входная точка)
├── config.json                      # Конфигурация (watchlist)
│
├── stock_data_manager.py            # Загрузка данных
├── technical_analysis.py            # Анализ индикаторов
├── report_generator.py              # Создание отчётов
│
├── stock_data/                      # CSV данные
│   ├── SBER_full.csv
│   ├── GAZP_full.csv
│   └── ...
│
├── reports/                         # Markdown отчёты
│   ├── report_20240113_101530.md
│   └── ...
│
├── requirements.txt                 # Зависимости
├── CLI_GUIDE.md                     # Справка по CLI
├── GETTING_STARTED.md               # Этот файл
├── README.md                        # Общее описание
├── TECHNICAL_ANALYSIS.md            # Справка по анализу
└── REPORT_GENERATOR.md              # Справка по отчётам
```

---

## 🔄 Типичный рабочий день

### Утро

```bash
# Обновить последние данные
python main.py update

# Создать утренний отчёт
python main.py analyze

# Посмотреть результаты в reports/report_*.md
```

### День

```bash
# Посмотреть информацию по интересующей акции
python main.py info GAZP

# Если нужны новые данные
python main.py update
```

### Вечер

```bash
# Итоговый отчёт
python main.py analyze

# Экспортировать результаты
# (Отчёты уже сохранены в reports/)
```

---

## 🎓 Обучение

### День 1: Основы

1. Прочитайте этот файл (GETTING_STARTED.md)
2. Запустите `python main.py list`
3. Запустите `python main.py update` (обновление данных)
4. Запустите `python main.py analyze` (создание отчёта)
5. Откройте созданный отчёт в `reports/`

### День 2: Управление

1. Прочитайте `CLI_GUIDE.md`
2. Добавьте новые акции: `python main.py add PLZL`
3. Удалите акцию: `python main.py remove TATN`
4. Посмотрите информацию: `python main.py info SBER`

### День 3: Понимание анализа

1. Прочитайте `TECHNICAL_ANALYSIS.md`
2. Прочитайте `REPORT_GENERATOR.md`
3. Изучите содержимое отчётов
4. Поймите смысл индикаторов (EMA, RSI, поддержка/сопротивление)

### День 4: Расширение

1. Используйте классы в своих скриптах
2. Модифицируйте исходный код под свои нужды
3. Интегрируйте с другими системами

---

## 🔧 Конфигурация

### config.json

```json
{
  "watchlist": ["SBER", "GAZP", "LKOH", "NVTK", "TATN"],
  "last_updated": null,
  "last_report": null,
  "settings": {
    "auto_update": false,
    "report_format": "markdown",
    "theme": "default"
  }
}
```

### Редактирование

Вы можете отредактировать watchlist вручную:

```json
{
  "watchlist": ["SBER", "GAZP", "PLZL", "PHOR"]
}
```

Или использовать CLI:

```bash
python main.py add PLZL
python main.py remove NVTK
```

---

## 🚀 Автоматизация

### Linux/Mac: Cron

```bash
# Открыть crontab
crontab -e

# Добавить строку (ежедневно в 11:00)
0 11 * * * cd /home/roman/projects/ai/stock_analyzer && python main.py update && python main.py analyze

# Или несколько раз в день (каждые 4 часа)
0 9,13,17,21 * * * cd /home/roman/projects/ai/stock_analyzer && python main.py update
```

### Windows: Task Scheduler

1. Откройте Task Scheduler
2. Создайте новую задачу
3. Действие: Запустить программу
4. Программа: `python.exe`
5. Аргументы: `main.py update`
6. Рабочая папка: `/home/roman/projects/ai/stock_analyzer`
7. Расписание: на ваш вкус

### Скрипт для автоматизации

Создайте `auto_update.py`:

```python
#!/usr/bin/env python3
import subprocess
from datetime import datetime

print(f"[{datetime.now()}] Запуск автоматического обновления...")

# Обновление
subprocess.run(["python", "main.py", "update"])

# Анализ
subprocess.run(["python", "main.py", "analyze"])

print(f"[{datetime.now()}] Готово!")
```

Запуск:
```bash
python auto_update.py
```

---

## 📊 Примеры результатов

### После `python main.py update`

```
============================================================
📥 ОБНОВЛЕНИЕ ДАННЫХ АКЦИЙ
============================================================

📊 Обновляем 5 акций: SBER, GAZP, LKOH, NVTK, TATN

✅ Успешно: 5
❌ Ошибок: 0

✅ Все данные обновлены!
```

### После `python main.py analyze`

```
============================================================
📊 АНАЛИЗ И СОЗДАНИЕ ОТЧЁТА
============================================================

🔍 Анализируем 5 акций...

✅ Отчёт создан: reports/report_20240113_101530.md

📄 Первая часть отчёта:
────────────────────────────────────────────────────────────
# Еженедельный анализ акций
...
────────────────────────────────────────────────────────────
```

---

## 🐛 Обычные ошибки

### "ModuleNotFoundError: No module named 'pandas'"

**Решение:**
```bash
pip install -r requirements.txt
```

### "Acция не найдена"

**Решение:** Проверьте правильность тикера:
```bash
# Правильные тикеры
python main.py add GAZP    # ✅
python main.py add SBER    # ✅

# Неправильные
python main.py add gazp123 # ❌
```

### "Нет данных для анализа"

**Решение:** Сначала обновите:
```bash
python main.py update
python main.py analyze
```

### "config.json повреждён"

**Решение:** Удалите и пересоздайте:
```bash
rm config.json
python main.py list
```

---

## 💡 Советы

1. **Регулярно обновляйте** данные (ежедневно лучше)
2. **Проверяйте отчёты** каждую неделю
3. **Экспортируйте важные** отчёты
4. **Комбинируйте** с собственным анализом
5. **Не полагайтесь** только на сигналы программы

---

## 📚 Справочная информация

### Все команды

```bash
python main.py -h                    # Справка
python main.py update -h             # Справка по команде
python main.py add -h
python main.py analyze -h
python main.py list -h
python main.py remove -h
python main.py info -h
python main.py status -h
```

### Все документы

- `README.md` - Общее описание проекта
- `QUICKSTART.md` - Быстрый старт (старого проекта)
- `TECHNICAL_ANALYSIS.md` - Справка по техническому анализу
- `REPORT_GENERATOR.md` - Справка по отчётам
- `CLI_GUIDE.md` - Полное руководство по CLI
- `GETTING_STARTED.md` - Этот файл

---

## ✨ Возможности

✅ Загрузка данных с API Мосбиржи  
✅ Технический анализ (EMA, RSI, поддержка/сопротивление, объёмы)  
✅ Создание красивых markdown-отчётов  
✅ Управление списком акций  
✅ CLI интерфейс  
✅ JSON конфигурация  
✅ Полное логирование  

---

## 🎯 Следующие шаги

1. ✅ Установить программу
2. ✅ Запустить первый раз
3. ✅ Прочитать `CLI_GUIDE.md`
4. ✅ Прочитать `TECHNICAL_ANALYSIS.md`
5. ✅ Настроить свой watchlist
6. ✅ Использовать регулярно

---

## 🆘 Помощь

Если что-то не работает:

1. Проверьте Python версию: `python --version` (должен быть 3.7+)
2. Проверьте зависимости: `pip install -r requirements.txt`
3. Читайте логи и сообщения об ошибках внимательно
4. Проверьте файл конфигурации: `cat config.json`
5. Посмотрите справку: `python main.py -h`

---

**Добро пожаловать в Stock Analyzer!** 🚀

Начните с:
```bash
python main.py list
python main.py update
python main.py analyze
```

Удачи в анализе акций! 📈

