# 📊 Technical Analysis - Модуль технического анализа

Модуль для расчета технических индикаторов и анализа акций.

## 📦 Установка

```bash
pip install -r requirements.txt
```

**Зависимости:**
- pandas (2.0.3) - обработка данных
- numpy (1.24.3) - численные расчеты
- ta (0.11.0) - техническая аналитика (опционально)

## 🎯 Основные функции

### 1. `calculate_ema(df, periods=[20, 50, 200])`

Расчет экспоненциального скользящего среднего (EMA).

**Параметры:**
- `df` - DataFrame с колонкой CLOSE
- `periods` - список периодов [20, 50, 200, ...]

**Возвращает:** DataFrame с колонками EMA_20, EMA_50, EMA_200

**Пример:**
```python
from technical_analysis import TechnicalAnalyzer
import pandas as pd

df = pd.read_csv('stock_data/SBER_full.csv', parse_dates=['DATE'])
analyzer = TechnicalAnalyzer()
df = analyzer.calculate_ema(df, periods=[20, 50, 200])

print(df[['DATE', 'CLOSE', 'EMA_20', 'EMA_50', 'EMA_200']].tail())
```

**Использование:**
- EMA 20: краткосрочный тренд
- EMA 50: среднесрочный тренд
- EMA 200: долгосрочный тренд

---

### 2. `calculate_rsi(df, period=14)`

Расчет индекса относительной силы (RSI).

**Параметры:**
- `df` - DataFrame с колонкой CLOSE
- `period` - период расчета (обычно 14)

**Возвращает:** DataFrame с колонкой RSI

**Интерпретация:**
- **RSI > 70** - ПЕРЕКУПЛЕНО (overbought), возможен спад
- **RSI < 30** - ПЕРЕПРОДАНО (oversold), возможен рост
- **30 < RSI < 70** - нейтральная зона

**Пример:**
```python
df = pd.read_csv('stock_data/GAZP_full.csv', parse_dates=['DATE'])
df = analyzer.calculate_rsi(df, period=14)

rsi = df['RSI'].iloc[-1]
if rsi > 70:
    print("Перекуплено - возможен спад")
elif rsi < 30:
    print("Перепродано - возможен рост")
else:
    print("Нейтральная зона")
```

---

### 3. `find_support_resistance(df, window=20)`

Определение уровней поддержки и сопротивления.

**Параметры:**
- `df` - DataFrame с колонками HIGH, LOW
- `window` - окно для поиска экстремумов (обычно 20)

**Возвращает:** Dict с уровнями

**Структура результата:**
```python
{
    'support': 105.30,           # Уровень поддержки
    'resistance': 115.50,        # Уровень сопротивления
    'support_levels_count': 12,  # Количество найденных уровней поддержки
    'resistance_levels_count': 8 # Количество найденных уровней сопротивления
}
```

**Пример:**
```python
sr = analyzer.find_support_resistance(df, window=20)
print(f"Поддержка: {sr['support']:.2f}")
print(f"Сопротивление: {sr['resistance']:.2f}")
```

---

### 4. `detect_trend(df)`

Определение текущего тренда.

**Возвращает:** Dict с информацией о тренде

**Структура результата:**
```python
{
    'trend': 'up',                    # up/down/sideways
    'strength': 'strong',             # strong/moderate/weak
    'current_price': 105.50,
    'ma_20': 104.30,
    'ma_50': 103.20,
    'angle': 0.45,                    # Угол наклона цены
    'above_ma20': True,               # Цена выше MA20?
    'above_ma50': True                # Цена выше MA50?
}
```

**Логика определения тренда:**
- **UP (сильный):** Цена > MA20 > MA50
- **UP (средний):** Цена > MA20 и Цена > MA50
- **DOWN (сильный):** Цена < MA20 < MA50
- **DOWN (средний):** Цена < MA20 и Цена < MA50
- **SIDEWAYS:** остальные случаи

**Пример:**
```python
trend = analyzer.detect_trend(df)
print(f"Тренд: {trend['trend'].upper()}")
print(f"Сила: {trend['strength']}")
print(f"Выше MA20: {trend['above_ma20']}")
print(f"Выше MA50: {trend['above_ma50']}")
```

---

### 5. `calculate_volume_profile(df, bins=20)`

Анализ профиля объемов.

**Параметры:**
- `df` - DataFrame с колонками CLOSE, VOLUME
- `bins` - количество ценовых уровней

**Возвращает:** Dict с анализом объемов

**Структура результата:**
```python
{
    'total_volume': 1250000000,       # Всего объема
    'avg_volume': 8500000,            # Средний объем
    'max_volume': 25000000,           # Максимум объема
    'min_volume': 2000000,            # Минимум объема
    'point_of_control': 105.50,       # Уровень максимальной активности
    'volume_trend': 'increasing',     # increasing/decreasing
    'volume_by_price': [              # Объемы по ценовым уровням
        {'price_level': 100.50, 'volume': 5000000},
        {...}
    ]
}
```

**Point of Control (POC)** - это ценовой уровень с наибольшим объемом торгов. Часто выступает поддержкой/сопротивлением.

**Пример:**
```python
vol = analyzer.calculate_volume_profile(df, bins=20)
print(f"Средний объем: {vol['avg_volume']:,.0f}")
print(f"Point of Control: {vol['point_of_control']:.2f}")
print(f"Тренд объема: {vol['volume_trend']}")
```

---

### 6. `analyze_stock(ticker, csv_path=None)`

**ПОЛНЫЙ АНАЛИЗ** акции - все индикаторы в одном вызове!

**Параметры:**
- `ticker` - тикер акции (например, 'SBER')
- `csv_path` - путь к CSV (если None, ищет в stock_data/{ticker}_full.csv)

**Возвращает:** Полный Dict с метриками

**Структура результата:**
```python
{
    'ticker': 'SBER',
    'data_points': 252,                    # Количество дней
    'date_from': '2023-11-13',
    'date_to': '2024-11-13',
    'current_price': 105.50,
    'price_change': 5.50,                  # Абсолютное изменение
    'price_change_pct': 5.50,              # % изменение
    
    'technical_indicators': {
        'ema_20': 104.30,
        'ema_50': 103.20,
        'ema_200': 102.10,
        'rsi': 65.50,
        'rsi_signal': 'overbought'         # overbought/oversold/neutral
    },
    
    'support_resistance': {
        'support': 100.50,
        'resistance': 110.50,
        'support_levels_count': 12,
        'resistance_levels_count': 8
    },
    
    'trend': {
        'trend': 'up',
        'strength': 'strong',
        'current_price': 105.50,
        'ma_20': 104.30,
        'ma_50': 103.20,
        'angle': 0.45,
        'above_ma20': True,
        'above_ma50': True
    },
    
    'volume': {
        'total_volume': 1250000000,
        'avg_volume': 8500000,
        'max_volume': 25000000,
        'min_volume': 2000000,
        'point_of_control': 105.50,
        'volume_trend': 'increasing',
        'volume_by_price': [...]
    }
}
```

**Пример:**
```python
result = analyzer.analyze_stock('SBER')

print(f"Цена: {result['current_price']:.2f}")
print(f"Изменение: {result['price_change_pct']:+.2f}%")
print(f"Тренд: {result['trend']['trend'].upper()}")
print(f"RSI: {result['technical_indicators']['rsi']:.2f}")
```

---

## 📖 Примеры использования

### Быстрый старт

```python
from technical_analysis import TechnicalAnalyzer

analyzer = TechnicalAnalyzer()

# Полный анализ
result = analyzer.analyze_stock('SBER')
print(result)
```

### Пример 1: EMA анализ

```python
import pandas as pd
from technical_analysis import TechnicalAnalyzer

df = pd.read_csv('stock_data/GAZP_full.csv', parse_dates=['DATE'])
analyzer = TechnicalAnalyzer()

# Рассчитываем EMA
df = analyzer.calculate_ema(df, periods=[20, 50, 200])

# Проверяем последние значения
print(df[['DATE', 'CLOSE', 'EMA_20', 'EMA_50', 'EMA_200']].tail())

# Позиция цены
close = df['CLOSE'].iloc[-1]
ema20 = df['EMA_20'].iloc[-1]

if close > ema20:
    print(f"Цена выше EMA20 на {close - ema20:.2f}")
else:
    print(f"Цена ниже EMA20 на {ema20 - close:.2f}")
```

### Пример 2: RSI сигналы

```python
df = pd.read_csv('stock_data/SBER_full.csv', parse_dates=['DATE'])
df = analyzer.calculate_rsi(df, period=14)

rsi = df['RSI'].iloc[-1]

if rsi > 70:
    signal = "🔴 ПЕРЕКУПЛЕНО - вероятна коррекция вниз"
elif rsi < 30:
    signal = "🟢 ПЕРЕПРОДАНО - вероятен рост"
else:
    signal = "⚪ Нейтральная зона"

print(f"RSI: {rsi:.2f}")
print(f"Сигнал: {signal}")
```

### Пример 3: Тренд анализ

```python
tickers = ['SBER', 'GAZP', 'LKOH']

for ticker in tickers:
    result = analyzer.analyze_stock(ticker)
    
    trend_info = result['trend']
    symbol = "📈" if trend_info['trend'] == 'up' else "📉"
    
    print(f"{ticker} {symbol} {trend_info['trend'].upper()} ({trend_info['strength']})")
    print(f"  Цена: {result['current_price']:.2f}")
    print(f"  Выше MA20: {trend_info['above_ma20']}")
```

### Пример 4: Поддержка/Сопротивление

```python
sr = analyzer.find_support_resistance(df, window=20)

support = sr['support']
resistance = sr['resistance']
current = df['CLOSE'].iloc[-1]

distance_to_res = resistance - current
distance_to_sup = current - support

print(f"Поддержка: {support:.2f} ({distance_to_sup:.2f} ниже)")
print(f"Сопротивление: {resistance:.2f} ({distance_to_res:.2f} выше)")
print(f"Диапазон: {resistance - support:.2f}")
```

### Пример 5: Объемный анализ

```python
vol = analyzer.calculate_volume_profile(df, bins=25)

print(f"Средний объем: {vol['avg_volume']:,.0f}")
print(f"Тренд объема: {vol['volume_trend']}")
print(f"POC (Point of Control): {vol['point_of_control']:.2f}")

# Сравнение с историческим средним
recent_avg = df['VOLUME'].tail(5).mean()
ratio = recent_avg / vol['avg_volume']
print(f"Последний объем/историческое среднее: {ratio:.2f}x")
```

### Пример 6: Сравнение акций

```python
tickers = ['SBER', 'GAZP', 'LKOH']

for ticker in tickers:
    result = analyzer.analyze_stock(ticker)
    
    rsi = result['technical_indicators']['rsi']
    trend = result['trend']['trend']
    price_change = result['price_change_pct']
    
    print(f"{ticker}: {trend.upper()} | RSI={rsi:.0f} | Изменение={price_change:+.2f}%")
```

---

## 🔧 Запуск примеров

```bash
python technical_example.py
```

Там 7 полных примеров:
1. Полный анализ одной акции
2. Расчет EMA
3. Анализ RSI
4. Определение тренда
5. Поддержка/сопротивление
6. Анализ объемов
7. Сравнение акций

---

## 📊 Интерпретация индикаторов

### EMA (Exponential Moving Average)

| Сценарий | Значение |
|----------|----------|
| **Восходящий тренд** | Цена > EMA20 > EMA50 > EMA200 |
| **Нисходящий тренд** | Цена < EMA20 < EMA50 < EMA200 |
| **Коррекция в тренде** | Цена пересекает EMA (потом возвращается) |
| **Разворот** | EMA пересекают друг друга |

### RSI (Relative Strength Index)

| Значение | Сигнал |
|----------|--------|
| **> 70** | 🔴 Перекуплено (overbought) |
| **60-70** | 🟡 Сильный восходящий тренд |
| **50-60** | ⚪ Восходящий тренд |
| **40-50** | ⚪ Нисходящий тренд |
| **30-40** | 🟡 Сильный нисходящий тренд |
| **< 30** | 🟢 Перепродано (oversold) |

### Поддержка/Сопротивление

- **Поддержка** - уровень, где цена обычно отскакивает вверх
- **Сопротивление** - уровень, где цена обычно отскакивает вниз
- Используются для определения точек входа/выхода

### Профиль объемов

- **Высокий объем** - зона активности трейдеров
- **Низкий объем** - зона апатии
- **POC** - часто выступает поддержкой/сопротивлением
- **Растущий объем** - подтверждение тренда

---

## ⚙️ Рекомендации по использованию

### Для торговли на краткосрочных отскоках
```python
# Используйте: EMA20, RSI, поддержку/сопротивление
```

### Для определения общего тренда
```python
# Используйте: EMA50/200, определение тренда
```

### Для входа в позицию
```python
# Условие: RSI < 30 (перепродано) И цена выше поддержки И восходящий тренд
```

### Для выхода из позиции
```python
# Условие: RSI > 70 (перекуплено) ИЛИ цена ниже сопротивления
```

---

## 🐛 Примечания

- Все функции обрабатывают ошибки и логируют их
- Функции работают без ta-lib (встроенная реализация на numpy/pandas)
- Используется pandas.DataFrame в качестве стандарта
- Все цены и объемы - float типы

---

## 📈 Интеграция с stock_data_manager

```python
from stock_data_manager import StockDataManager
from technical_analysis import TechnicalAnalyzer

# Загружаем данные
manager = StockDataManager()
manager.update_watchlist(['SBER', 'GAZP'])

# Анализируем
analyzer = TechnicalAnalyzer()
result = analyzer.analyze_stock('SBER')

# Используем результат
if result['trend']['trend'] == 'up' and result['technical_indicators']['rsi'] < 50:
    print("Хорошая точка входа на восходящем тренде!")
```

---

**Версия:** 1.0.0  
**Дата:** 13 Ноября 2024  
**Статус:** ✅ Production Ready

