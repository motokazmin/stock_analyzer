# 📈 Stock Data Manager - Проект

## Описание проекта

**Stock Data Manager** - это полнофункциональная Python-система для автоматической загрузки, управления и анализа данных акций с Московской биржи (Мосбиржи) через официальный API ISS.

Проект предоставляет удобный и надежный способ получить исторические данные по акциям в формате CSV для дальнейшего анализа и торговли.

## 🎯 Основные возможности

### Управление данными
- ✅ Автоматическая загрузка данных с API Мосбиржи
- ✅ Инкрементальное обновление (только новые данные)
- ✅ Сохранение в CSV формате
- ✅ Автоматическое определение последней даты

### Надежность
- ✅ Автоматические повторные попытки при сбое
- ✅ Обработка ошибок API
- ✅ Валидация данных
- ✅ Логирование всех операций

### Удобство
- ✅ Простой API класса
- ✅ Пакетная обработка списков акций
- ✅ Получение статистики
- ✅ Примеры использования

### Анализ
- ✅ Технические индикаторы
- ✅ Расчет волатильности
- ✅ Сравнение нескольких акций
- ✅ Экспорт данных с анализом

## 📁 Структура проекта

```
trading/
│
├── 📄 Основные файлы
├── stock_data_manager.py (395 строк)    # Главный класс
├── data_analyzer.py (280 строк)         # Анализ данных
├── config.py (80 строк)                 # Конфигурация
│
├── 🚀 Использование
├── example_usage.py                     # 5 полных примеров
├── daily_update.py                      # Ежедневное обновление
├── test_manager.py                      # 5 интеграционных тестов
│
├── 📚 Документация
├── README.md (350 строк)                # Полная документация
├── QUICKSTART.md (150 строк)            # Быстрый старт
├── INSTALL.md (250 строк)               # Установка
├── PROJECT_OVERVIEW.md                  # Этот файл
│
├── ⚙️ Конфигурация
├── requirements.txt                     # Зависимости
│
└── 📦 Генерируемые файлы
    ├── stock_data_manager.log           # Логи
    └── stock_data/
        ├── SBER_full.csv
        ├── GAZP_full.csv
        └── ...
```

## 🔧 Стек технологий

| Компонент | Версия | Назначение |
|-----------|--------|-----------|
| Python | 3.7+ | Язык программирования |
| requests | 2.31.0 | HTTP запросы к API |
| pandas | 2.0.3 | Обработка таблиц данных |
| urllib3 | 2.0.4 | HTTP утилиты |

## 📊 Классы и методы

### StockDataManager

**Основные методы:**

```python
# Загрузка данных
download_stock_data(ticker, from_date, to_date) -> pd.DataFrame

# Управление данными
update_watchlist(tickers_list) -> Dict[str, bool]
save_to_csv(ticker, data) -> bool
get_data(ticker) -> pd.DataFrame

# Аналитика
get_statistics(ticker) -> Dict
```

### DataAnalyzer

**Методы анализа:**

```python
# Технические индикаторы
get_daily_changes(ticker) -> pd.DataFrame
get_moving_average(ticker, window=20) -> pd.DataFrame
get_volatility(ticker, window=20) -> float

# Сравнение
compare_tickers(tickers) -> pd.DataFrame
price_range(ticker) -> Dict

# Экспорт
export_comparison(tickers, filename)
export_ticker_data(ticker, output_dir)
```

## 🚀 Быстрый старт

### 1. Установка

```bash
cd /home/roman/projects/ai/trading
pip install -r requirements.txt
```

### 2. Загрузка данных

```python
from stock_data_manager import StockDataManager

manager = StockDataManager()
manager.update_watchlist(['SBER', 'GAZP', 'LKOH'])
```

### 3. Анализ

```python
from data_analyzer import DataAnalyzer

analyzer = DataAnalyzer()
stats = analyzer.compare_tickers(['SBER', 'GAZP'])
print(stats)
```

## 💡 Примеры использования

### Пример 1: Простая загрузка

```python
manager = StockDataManager()
data = manager.download_stock_data('SBER', '2024-01-01', '2024-12-31')
manager.save_to_csv('SBER', data)
```

### Пример 2: Ежедневное обновление

```python
# daily_update.py
manager = StockDataManager()
manager.update_watchlist(['SBER', 'GAZP', 'LKOH', 'NVTK', 'TATN'])
```

### Пример 3: Получение статистики

```python
stats = manager.get_statistics('SBER')
print(f"Ср. цена: {stats['avg_price']:.2f} ₽")
print(f"Макс: {stats['max_price']:.2f} ₽")
print(f"Мин: {stats['min_price']:.2f} ₽")
```

### Пример 4: Анализ волатильности

```python
analyzer = DataAnalyzer()
vol = analyzer.get_volatility('GAZP', window=20)
print(f"Волатильность: {vol:.2f}%")
```

### Пример 5: Сравнение акций

```python
df = analyzer.compare_tickers(['SBER', 'GAZP', 'LKOH'])
print(df)
```

## 📈 Особенности реализации

### Интеллектуальное обновление
- Система автоматически определяет последнюю загруженную дату
- Загружает только новые данные (экономит трафик)
- Объединяет старые и новые данные без дубликатов

### Обработка ошибок
- 3 автоматических повтора при сбое API
- Обработка таймаутов и разрывов соединения
- Валидация формата данных

### Логирование
- Логирование в консоль и файл
- Структурированный формат с датой/временем
- Разные уровни логирования (INFO, WARNING, ERROR)

### Производительность
- Пакетная загрузка 100 записей за раз
- Кэширование соединений
- Эффективная работа с Pandas

## 🔌 API Мосбиржи

**Используется endpoint:**
```
https://iss.moex.com/iss/history/engines/stock/markets/shares/securities/{ticker}.json
```

**Параметры запроса:**
- `start` - начало пагинации
- `limit` - количество записей (макс 100)

**Формат ответа:**
```json
{
  "history": {
    "columns": ["TRADEDATE", "OPEN", "HIGH", "LOW", "CLOSE", "VOLUME"],
    "data": [
      ["2024-01-01", 100.5, 102.0, 99.5, 101.5, 12500000],
      ...
    ]
  }
}
```

## 📊 Формат CSV

**Сохранение в CSV:**
```csv
DATE,OPEN,HIGH,LOW,CLOSE,VOLUME
2024-01-01,105.50,106.25,105.00,106.00,12500000
2024-01-02,106.00,107.50,105.75,107.25,15300000
```

**Разделитель:** `,` (запятая)  
**Кодировка:** UTF-8  
**Директория:** `stock_data/`

## 🧪 Тестирование

**5 интеграционных тестов:**

1. ✅ Инициализация менеджера
2. ✅ Загрузка данных (SBER)
3. ✅ Сохранение и загрузка CSV (GAZP)
4. ✅ Получение статистики (LKOH)
5. ✅ Обновление списка акций (NVTK, TATN)

**Запуск:**
```bash
python test_manager.py
```

## 📅 Автоматизация

### Linux/Mac (cron)
```bash
0 11 * * * cd /home/roman/projects/ai/trading && python daily_update.py
```

### Windows (Task Scheduler)
Используйте встроенный Task Scheduler для запуска `daily_update.py`

## 🎓 Примеры популярных акций

| Компания | Тикер | Сектор |
|----------|-------|--------|
| Сбербанк | SBER | Финансы |
| Газпром | GAZP | Энергетика |
| Лукойл | LKOH | Нефть |
| Новатэк | NVTK | Газ |
| Татнефть | TATN | Нефть |
| Полюс | PLZL | Золото |

## 💼 Примеры использования в реальных задачах

### Задача 1: Портфолио мониторинг
```python
manager = StockDataManager()
manager.update_watchlist(['SBER', 'GAZP', 'LKOH'])

for ticker in ['SBER', 'GAZP', 'LKOH']:
    stats = manager.get_statistics(ticker)
    print(f"{ticker}: Ср.цена={stats['avg_price']:.2f}")
```

### Задача 2: Техническая аналитика
```python
analyzer = DataAnalyzer()
ma = analyzer.get_moving_average('SBER', window=50)
volatility = analyzer.get_volatility('SBER')
```

### Задача 3: Сравнение инструментов
```python
df = analyzer.compare_tickers(['SBER', 'GAZP', 'LKOH'])
analyzer.export_comparison(['SBER', 'GAZP'], 'report.csv')
```

## 🐛 Известные ограничения

- API возвращает макс. 100 записей за раз (решено пагинацией)
- История доступна обычно за несколько лет
- Некоторые акции могут быть удалены из листинга

## 🔐 Безопасность

- Не требует ключей/паролей
- API Мосбиржи публичный
- Данные сохраняются локально
- Нет передачи личной информации

## 📞 Поддержка и помощь

**Документация:**
- 📖 `README.md` - полная справка
- 🚀 `QUICKSTART.md` - быстрый старт
- 💻 `example_usage.py` - примеры кода
- ⚙️ `INSTALL.md` - установка

**Тестирование:**
```bash
python test_manager.py
```

**Логи:**
```bash
tail -f stock_data_manager.log
```

## 📝 Лицензия

MIT License - свободное использование и модификация

## 🎯 Статус проекта

✅ **Завершено и протестировано**

Все требования реализованы:
- ✅ Класс StockDataManager
- ✅ Все методы согласно ТЗ
- ✅ Определение последней даты
- ✅ Обновление новых данных
- ✅ Обработка ошибок
- ✅ Логирование
- ✅ CSV формат

## 🚀 Следующие шаги

1. Установить зависимости: `pip install -r requirements.txt`
2. Запустить тесты: `python test_manager.py`
3. Загрузить данные: `python example_usage.py`
4. Использовать в своих проектах
5. Настроить ежедневное обновление через cron

---

**Готово к использованию!** 🎉

Используйте этот проект для:
- 📊 Анализа исторических данных акций
- 📈 Разработки торговых стратегий
- 💻 Автоматизации мониторинга портфеля
- 🤖 Машинного обучения на биржевых данных

Все файлы документированы и содержат примеры кода.

